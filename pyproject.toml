[project]
name = "hellofresh-workshops"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "google-adk",
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.12",
    "ruff-lsp>=0.0.62",
]
