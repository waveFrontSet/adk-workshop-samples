[project]
name = "adk-workshop-samples"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["google-adk"]

[dependency-groups]
dev = [
  "mypy>=2.1.0",
  "pytest>=9.0.3",
  "pytest-cov>=7.1.0",
  "ruff>=0.15.12",
  "ruff-lsp>=0.0.62",
]
